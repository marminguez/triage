{
    "name": "triage-core-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
        "start": "node backend/src/index.js",
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "axios": "^1.13.4",
        "cors": "^2.8.6",
        "express": "^5.2.1",
        "nodemon": "^3.1.11"
    },
    "engines": {
        "node": "18.x"
    }
}